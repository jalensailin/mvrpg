{{!-- Sheet Tab Navigation --}}
<div class="tab description-section sheet-body comic-frame flexcol {{tabs.description.cssClass}}"
     data-group="{{tabs.description.group}}"
     data-tab="{{tabs.description.id}}">
  {{editor enrichedDescription target="system.description" button="true" engine="prosemirror"}}
</div>
